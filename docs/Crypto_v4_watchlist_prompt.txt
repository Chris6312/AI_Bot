Use this version:

```text
You are generating a SINGLE JSON watchlist for an external trading bot that listens to Discord.

IMPORTANT FRESH-SCAN RULE:
Do NOT rely on any previous output, previous ranking, past watchlists, memory, earlier symbols, or prior session history.
You must perform a completely new scan of the Kraken app RIGHT NOW because top gainers, top losers, trending assets, and news can change instantly.
Treat every run as a fresh scan from scratch.

UNIVERSE RESTRICTION:
You must evaluate all of the following symbols and include every valid one in the final watchlist.
Do not consider any symbol outside this list, even if it appears in Kraken top gainers, top losers, trending, or news.

Required symbols to evaluate:
- BTC
- ETH
- USDT
- BNB
- XRP
- USDC
- SOL
- TRX
- DOGE
- USDS
- WBT
- ADA
- BCH
- HYPE
- WBTC
- LINK
- XMR
- USDE
- CC
- XLM

Inclusion rules:
- If a symbol from this list is available as a Kraken spot pair, include it in the final watchlist.
- If a symbol from this list is not available as a Kraken spot pair, skip it.
- Do NOT remove a symbol only because it is low conviction, a stablecoin, synthetic, wrapped, weak, or unsuitable for swing trading.
- Instead, include it anyway if Kraken spot supports it, score it honestly, assign the most appropriate setup_template and exit_template, and explain the low-conviction or low-suitability issues in ui_payload.
- The final watchlist should therefore contain every allowed symbol that is actually available on Kraken spot.

Use ONLY information available through this Kraken app right now:
- Spot pairs only
- top gainers
- top losers
- trending crypto assets
- latest news, narratives, and market insight for specific crypto symbols
- current Kraken context for symbols from the required list only

Do NOT use or invent:
- OHLCV or candle data
- exact support/resistance prices
- exact trigger prices
- order book, recent trades, spreads, or private account data
- stock symbols or stock analysis
- account balances, position sizing from balances, or exchange execution details
- any stale assumptions from earlier runs
- any symbol outside the required list

Goal:
Produce ONE JSON object that contains:
1. a bot_payload with only machine-usable fields
2. a ui_payload with explanation fields for sidebar / audit display

The downstream bot will:
- parse the JSON
- ignore ui_payload for execution
- fetch its own candles
- map setup_template and exit_template to hardcoded strategy logic

Important rules:
- Return RAW JSON only
- No markdown
- No prose outside the JSON
- Do not fabricate exact prices
- Do not include stocks
- Keep bot_payload lean and deterministic
- Put all narrative/explanatory text in ui_payload only
- Do not include avoid_symbols or any exclusion list
- Include every supported symbol from the required list
- Sort symbols by priority_rank ascending
- selected_count must exactly match the number of objects in bot_payload.symbols
- primary_focus must be a subset of included symbols only

STRICT PER-COIN DECISION RUBRIC:
For each required symbol, score the following from current Kraken app context only.

1. Presence score
- 3 = appears in trending and gainers
- 2 = appears in either trending or gainers
- 1 = appears in losers only
- 0 = not surfaced in Kraken lists but still may be evaluated from Kraken symbol news/context if available

2. Narrative score
- 3 = clearly constructive, fresh, supportive narrative
- 2 = mixed but still usable
- 1 = weak, vague, or low-conviction
- 0 = mostly negative or broken story

3. Momentum posture score
- 3 = leadership, persistent attention, active momentum
- 2 = fresh move but may need confirmation
- 1 = possible rebound only
- 0 = stale, noisy, unsuitable, or likely random

4. Crowding risk score
- 0 = low crowding
- 1 = moderate crowding
- 2 = high crowding
- 3 = extreme or parabolic crowding

5. Reversal quality score
- 3 = clear rebound candidate with usable narrative
- 2 = bounce possible but mixed
- 1 = weak bounce case
- 0 = no reason to prefer rebound setup

6. Inclusion confidence
- 3 = definitely include
- 2 = include if room
- 1 = fringe
- 0 = leave out

CORE SCORE:
Core Score = Presence score + Narrative score + Momentum posture score + Inclusion confidence

IMPORTANT OVERRIDE:
Do not use Inclusion confidence = 0 as a reason to exclude a supported required symbol.
All supported required symbols must still be included.
Inclusion confidence should still be scored honestly and used only for ranking priority and context quality.

SETUP ASSIGNMENT RULES:
Assign exactly one setup_template per included symbol using these rules in order.

1. trend_continuation
Assign if:
- Presence score >= 2
- Narrative score >= 2
- Momentum posture score = 3
- Crowding risk score <= 2

2. breakout_retest
Assign if:
- Presence score >= 2
- Narrative score >= 2
- Momentum posture score = 2
- Inclusion confidence >= 1

3. pullback_reclaim
Assign if:
- Presence score >= 1
- Narrative score >= 2
- Momentum posture score is 1 or 2
- and the symbol does not qualify for trend_continuation

4. mean_reversion_bounce
Assign if:
- the symbol appears in losers or Presence score = 1
- Reversal quality score >= 1
- Narrative score >= 0

5. range_breakout
Assign if:
- Presence score >= 0
- Narrative score >= 0
- Momentum posture score = 2
- and the symbol does not cleanly fit the other setup rules

DEFAULT ASSIGNMENT RULE:
If a supported required symbol does not cleanly fit any of the above:
- assign range_breakout for neutral / unclear watch candidates
- assign mean_reversion_bounce for weak or loser-style rebound candidates
- never leave setup_template blank

ENTRY STRATEGY INTERPRETATION:
The selected setup_template is the suggested entry strategy.
Use it to express the intended trade posture:
- trend_continuation = leadership continuation entry idea
- breakout_retest = fresh strength after confirmation entry idea
- pullback_reclaim = reclaim-strength entry idea
- mean_reversion_bounce = tactical rebound entry idea
- range_breakout = watch-for-expansion entry idea

EXIT TEMPLATE MAPPING:
Use these defaults unless Kraken context strongly suggests otherwise:
- trend_continuation -> trail_after_impulse
- breakout_retest -> scale_out_then_trail
- pullback_reclaim -> first_failed_follow_through
- mean_reversion_bounce -> time_stop_with_structure_check
- range_breakout -> scale_out_then_trail

EXIT STRATEGY INTERPRETATION:
The selected exit_template is the suggested exit strategy.

RISK FLAG RULES:
Add only allowed risk_flags and only when justified by the rubric:
- crowded_trade if Crowding risk score >= 2
- parabolic_recent_move if Crowding risk score = 3
- headline_sensitive if the thesis depends heavily on a recent narrative or news item
- low_conviction_news if Narrative score = 1
- weak_follow_through if Momentum posture score = 1
- mean_reversion_only if setup_template = mean_reversion_bounce
- reversal_not_confirmed if setup_template = mean_reversion_bounce
- reversal_not_confirmed may also be used for pullback_reclaim when confidence is mixed
- high_beta for smaller or more reactive alt names

TIER RULES:
- tier_1 = Core Score 10 to 12 and Inclusion confidence = 3
- tier_2 = Core Score 7 to 9
- tier_3 = Core Score 4 to 6
- tier_4 = Core Score 0 to 3

PRIORITY RANK RULES:
Rank all included supported symbols by:
1. higher Inclusion confidence
2. higher Core Score
3. higher Presence score
4. higher Narrative score
5. lower Crowding risk score

SPECIAL HANDLING RULES:
- Stablecoins such as USDT, USDC, USDS, and USDE should still be included if Kraken spot supports them, but should usually receive low inclusion confidence, conservative ranking, and explanatory notes that they are poor swing-trade candidates.
- Wrapped or tokenized assets such as WBTC and WBT should still be included if Kraken spot supports them, but should usually rank below cleaner primary asset alternatives unless Kraken context strongly favors them.
- If a symbol has little or no actionable Kraken context, still include it if supported, assign a conservative setup/exit pair, low ranking, and explain the limitations in ui_payload.notes.

Allowed setup_template values:
- breakout_retest
- pullback_reclaim
- trend_continuation
- mean_reversion_bounce
- range_breakout

Allowed exit_template values:
- scale_out_then_trail
- first_failed_follow_through
- sell_into_strength
- trail_after_impulse
- time_stop_with_structure_check

Allowed risk_flags:
- headline_sensitive
- high_beta
- parabolic_recent_move
- weak_follow_through
- mean_reversion_only
- crowded_trade
- low_conviction_news
- reversal_not_confirmed

Allowed bias values:
- bullish
- bearish
- neutral

Allowed market_regime values:
- risk_on
- mixed
- risk_off

Schema requirements:
- bot_payload.symbols must be a FLAT array, not tier buckets
- bot_payload.symbols must contain only these keys:
  - symbol
  - quote_currency
  - asset_class
  - enabled
  - trade_direction
  - priority_rank
  - tier
  - bias
  - setup_template
  - bot_timeframes
  - exit_template
  - max_hold_hours
  - risk_flags
- ui_payload.symbol_context is where you place explanation text
- Do not place thesis, why_now, notes, radar_bucket, or role inside bot_payload.symbols
- Do not include any field for avoid_symbols, avoid_context, excluded symbols, or rejected symbols
- Add every supported required symbol to bot_payload.symbols
- selected_count must equal the total number of supported required symbols included

Return JSON in exactly this structure:

{
  "schema_version": "bot_watchlist_v3",
  "generated_at_utc": "ISO-8601 UTC timestamp",
  "provider": "chatgpt_kraken_app",
  "scope": "crypto_only",
  "bot_payload": {
    "market_regime": "risk_on | mixed | risk_off",
    "symbols": [
      {
        "symbol": "BTC",
        "quote_currency": "USD",
        "asset_class": "crypto",
        "enabled": true,
        "trade_direction": "long",
        "priority_rank": 1,
        "tier": "tier_1",
        "bias": "bullish",
        "setup_template": "trend_continuation",
        "bot_timeframes": ["15m", "1h", "4h"],
        "exit_template": "trail_after_impulse",
        "max_hold_hours": 72,
        "risk_flags": ["crowded_trade"]
      }
    ]
  },
  "ui_payload": {
    "summary": {
      "selected_count": 0,
      "primary_focus": ["symbol1", "symbol2", "symbol3"],
      "regime_note": "short text"
    },
    "provider_limitations": [
      "short string",
      "short string"
    ],
    "symbol_context": {
      "BTC": {
        "radar_bucket": "trending | gainers | losers | none",
        "role": "market_anchor | high_beta_major | momentum_leader | narrative_leader | pullback_candidate | reversal_candidate | stablecoin | wrapped_proxy | low_conviction_watch",
        "thesis": "short text",
        "why_now": "short text",
        "notes": "short text"
      }
    }
  }
}
```
